#include<bits/stdc++.h>

//heap is a complete binary tree- where alll leve complte filled except last one
using namespace std;

class Heap{
    public:
    int arr[101];
    int size ;

    Heap(){
        size = 0;
    }

    void insert(int value) {
        size = size +1;
        int index = size;
        arr[index] = value;

        while(index>1){
            int pindex =index/2;

            ifarr[]
        }
    }
}

int main()